server:
  port: 8083

spring:
  application:
    name: MAProjectPaymentServices
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

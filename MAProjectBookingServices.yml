server:
  port: 8081

spring:
  application:
    name: MAProjectBookingServices
  datasource:
    url: jdbc:mysql://localhost:3306/booking_db
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8085

spring:
  application:
    name: Vehicle
  datasource:
    url: jdbc:mysql://localhost:3306/vehicle_db
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

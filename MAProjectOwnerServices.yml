server:
  port: 8082

spring:
  application:
    name: MAProjectOwnerServices
  datasource:
    url: jdbc:mysql://localhost:3306/owner_db
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

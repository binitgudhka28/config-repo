server:
  port: 8084

spring:
  application:
    name: MAProjectUserServices
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
